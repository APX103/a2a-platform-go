name: a2a-platform
host: 0.0.0.0
port: 18090

mysql:
  host: "mysql"
  port: 3306
  user: a2a
  password: a2a_secret_2024
  database: a2a_platform
  max_idle: 10
  max_open: 100

admin_token: "a2a-admin-token"

# Example builtin agents (uncomment and configure to use):
# builtin_agents:
#   - name: assistant
#     provider: openai          # openai or anthropic
#     base_url: https://api.openai.com
#     api_key: ${OPENAI_API_KEY}
#     model: gpt-4o
#     description: "General assistant"
#     system_prompt: "You are a helpful assistant."
#     max_tokens: 4096
#     max_tool_rounds: 10

# Example bridge agents (uncomment and configure to use):
# bridge_agents:
#   - name: my-llm
#     description: "Bridge to OpenAI-compatible API"
#     target:
#       http:
#         baseUrl: "http://localhost:8642"
#         headers:
#           Authorization: "Bearer ${LLM_API_KEY}"
#     skills:
#       - id: chat
#         name: Chat
#         description: "Send a message and get a response"
#         invoke:
#           type: http
#           method: POST
#           path: /v1/chat/completions
#           headers:
#             Content-Type: application/json
#           body:
#             model: "gpt-4o"
#             messages:
#               - role: user
#                 content: "{{inputText}}"
#             stream: false
#           response:
#             text: "{{output.choices.0.message.content}}"
